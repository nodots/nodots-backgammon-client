import {
  Game,
  GameError,
  GameErrorType,
  Color,
  CheckerboxPosition,
  MoveDirection,
  isColor,
} from './game'

export { GameError, isColor }
export type { Game, GameErrorType, Color, CheckerboxPosition, MoveDirection }
