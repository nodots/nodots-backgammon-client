import { Die, DieOrder, DieValue, isDieValue, isDieOrder, roll } from './die'
import { DiePair, Roll } from './die-pair'

export type {
  Die,
  DiePair,
  DieOrder,
  DieValue,
  Roll
}

export {
  isDieValue,
  isDieOrder,
  roll
}