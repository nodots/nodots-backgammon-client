import { Die, DieOrder, DieValue, isDieValue, isDieOrder, roll } from './die'
import { DiePair } from './die-pair'

export type {
  Die,
  DiePair,
  DieOrder,
  DieValue,
}

export {
  isDieValue,
  isDieOrder,
  roll
}