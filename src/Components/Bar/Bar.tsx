import { CheckerProps } from '../Checker/Checker'
import './Bar.scss'

interface BarProps {
  currentCheckers?: CheckerProps[]
}

const Bar = (prop: BarProps) => {
  return <div></div>
}

export default Bar