import { initBoardState } from './board.state'

import {
  POINT_COUNT,
  Board,
  getCheckerboxes,
  sanityCheckBoard,
  getCheckerboxCoordinates,
  getPipCountForPlayer,
  isBoard,
} from './types'

export {
  initBoardState,
  POINT_COUNT,
  getCheckerboxes,
  sanityCheckBoard,
  getCheckerboxCoordinates,
  getPipCountForPlayer,
  isBoard,
}

export type { Board }
